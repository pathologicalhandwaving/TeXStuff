\usepackage{enumerate}


% for capital roman numbers
\begin{enumerate}[I]  
\item
\end{enumerate}

% for small alpha-characters in brackets
\begin{enumerate}[(a)]%for small alpha-characters within brackets.
\item
\end{enumerate}

% Itemize environment can be changed to use different shapes
% You can do this at each level of indentation {\labelitemi}
\renewcommand{\labelitemi}{$\bullet$}
\renewcommand{\labelitemii}{$\cdot$}
\renewcommand{\labelitemiii}{$\diamond$}

% Common Shapes
$\bullet$ (\bullet), 
$\cdot$ (\cdot), 
$\diamond$ (\diamond), 
$-$ (-), 
$\ast$ (\ast)  
$\circ$ (\circ)

% Usage
\renewcommand{\labelitemiv}{$\ast$}

% Description
\begin{description}
\item[Described Thing] The description of the described thing.
\end{description}

% You can space out items in lists
\begin{itemize}\itemsep2pt
\item
\end{itemize}

% Numbered list 
\alph	Lowercase letter (a, b, c, ...)
\Alph	Uppercase letter (A, B, C, ...)
\arabic	Arabic number (1, 2, 3, ...)
\roman	Lowercase Roman numeral (i, ii, iii, ...)
\Roman	Uppercase Roman numeral (I, II, III, ...)
